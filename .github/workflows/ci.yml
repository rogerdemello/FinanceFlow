"""
This workflow has been intentionally disabled.
It was kept here for history but will not trigger any runs.
To re-enable CI, restore a proper `on:` trigger (push / pull_request).
"""

name: CI (disabled)

# No triggers defined — workflow is disabled
on: []
